public class EqualSumChecker {
    public static boolean hasEqualSum(int first, int seccond, int third) {
        int sum = first + seccond;
        if (sum == third) {
            return true;
        } return false;
    }
}
